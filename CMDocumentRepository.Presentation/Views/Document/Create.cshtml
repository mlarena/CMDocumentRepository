@model CMDocumentRepository.Application.DTOs.CreateDocumentDto

@{
    ViewData["Title"] = "Создание документа";
}

<h4>Создание нового документа</h4>
<hr />

<div class="row">
    <div class="col-md-8">
        <form asp-action="Create" enctype="multipart/form-data">
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">Основная информация</h5>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label asp-for="Title" class="form-label">Название документа *</label>
                        <input asp-for="Title" class="form-control" placeholder="Введите название документа" />
                        <span asp-validation-for="Title" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Description" class="form-label">Описание</label>
                        <textarea asp-for="Description" class="form-control" rows="3" placeholder="Краткое описание документа"></textarea>
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="DocumentTypeId" class="form-label">Тип документа *</label>
                            <select asp-for="DocumentTypeId" class="form-select">
                                <option value="">Выберите тип</option>
                                @if (ViewBag.DocumentTypes != null)
                                {
                                    foreach (var type in (List<CMDocumentRepository.Application.DTOs.DocumentTypeDto>)ViewBag.DocumentTypes)
                                    {
                                        <option value="@type.Id">@type.Name (@type.Code)</option>
                                    }
                                }
                            </select>
                            <span asp-validation-for="DocumentTypeId" class="text-danger"></span>
                        </div>

                        <div class="col-md-6 mb-3">
                            <label asp-for="CategoryId" class="form-label">Категория *</label>
                            <select asp-for="CategoryId" class="form-select">
                                <option value="">Выберите категорию</option>
                                @if (ViewBag.Categories != null)
                                {
                                    foreach (var cat in (List<CMDocumentRepository.Application.DTOs.CategoryDto>)ViewBag.Categories)
                                    {
                                        <option value="@cat.Id">@cat.Name</option>
                                    }
                                }
                            </select>
                            <span asp-validation-for="CategoryId" class="text-danger"></span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">Файл</h5>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label class="form-label">Загрузить файл</label>
                        <input type="file" name="File" class="form-control" accept=".pdf,.doc,.docx,.xls,.xlsx,.txt,.jpg,.png" />
                        <small class="text-muted">Допустимые форматы: PDF, DOC, DOCX, XLS, XLSX, TXT, JPG, PNG. Максимум 50 МБ</small>
                    </div>
                </div>
            </div>

            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">Сроки действия</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="ValidFrom" class="form-label">Дата начала</label>
                            <input asp-for="ValidFrom" type="date" class="form-control" />
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="ValidUntil" class="form-label">Дата окончания</label>
                            <input asp-for="ValidUntil" type="date" class="form-control" />
                        </div>
                    </div>
                </div>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-check-circle"></i> Создать
                </button>
                <a asp-action="Index" class="btn btn-outline-secondary">Отмена</a>
            </div>
        </form>
    </div>
</div>
